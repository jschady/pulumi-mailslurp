// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as enums from "./types/enums";
import * as utilities from "./utilities";

/**
 * The function reads one MailSlurp custom domain by its identifier. The
 * result carries the DNS records that the domain needs, so a stack can
 * create them with a DNS provider. The function reads the domain and
 * changes nothing.
 */
export function getDomain(args: GetDomainArgs, opts?: pulumi.InvokeOptions): Promise<GetDomainResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("mailslurp:index:getDomain", {
        "checkForErrors": args.checkForErrors,
        "domainId": args.domainId,
    }, opts);
}

export interface GetDomainArgs {
    /**
     * If you set this property, MailSlurp checks the domain for DNS record
     * problems. The default is `false`.
     */
    checkForErrors?: boolean;
    /**
     * The identifier of the domain to read. MailSlurp assigns this value when
     * it creates the domain.
     */
    domainId: string;
}

export interface GetDomainResult {
    /**
     * The identifier of the inbox that receives the email of every unmatched
     * address.
     */
    readonly catchAllInboxId?: string;
    /**
     * The time when MailSlurp created the domain.
     */
    readonly createdAt: string;
    /**
     * The DKIM values of the domain. Add one CNAME record for each value.
     */
    readonly dkimTokens: string[];
    /**
     * The custom domain name.
     */
    readonly domain: string;
    /**
     * The identifier of the domain.
     */
    readonly domainId: string;
    /**
     * The DNS records that the domain needs. Loop over this list to create the
     * records with a DNS provider.
     */
    readonly domainNameRecords: outputs.DomainNameRecordResult[];
    /**
     * The domain type.
     */
    readonly domainType: enums.DomainType;
    /**
     * The message that names the duplicate DNS records.
     */
    readonly duplicateRecordsMessage?: string;
    /**
     * MailSlurp reports whether the domain has a duplicate DNS record.
     */
    readonly hasDuplicateRecords: boolean;
    /**
     * MailSlurp reports whether the domain misses a required DNS record.
     */
    readonly hasMissingRecords: boolean;
    /**
     * MailSlurp reports whether the domain passed the DNS checks.
     */
    readonly isVerified: boolean;
    /**
     * The message that names the missing DNS records.
     */
    readonly missingRecordsMessage?: string;
    /**
     * The time when MailSlurp last changed the domain.
     */
    readonly updatedAt: string;
    /**
     * The value that proves you own the domain. Add it to the DNS records of
     * the domain.
     */
    readonly verificationToken: string;
}
/**
 * The function reads one MailSlurp custom domain by its identifier. The
 * result carries the DNS records that the domain needs, so a stack can
 * create them with a DNS provider. The function reads the domain and
 * changes nothing.
 */
export function getDomainOutput(args: GetDomainOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetDomainResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("mailslurp:index:getDomain", {
        "checkForErrors": args.checkForErrors,
        "domainId": args.domainId,
    }, opts);
}

export interface GetDomainOutputArgs {
    /**
     * If you set this property, MailSlurp checks the domain for DNS record
     * problems. The default is `false`.
     */
    checkForErrors?: pulumi.Input<boolean | undefined>;
    /**
     * The identifier of the domain to read. MailSlurp assigns this value when
     * it creates the domain.
     */
    domainId: pulumi.Input<string>;
}
